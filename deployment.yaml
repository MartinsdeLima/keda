apiVersion: apps/v1
kind: Deployment
metadata:
  name: keda-php-demo
  namespace: keda-demo
spec:
  replicas: 0
  selector:
    matchLabels:
      app: keda-php-demo
  template:
    metadata:
      labels:
        app: keda-php-demo
    spec:
      containers:
        - name: keda-php-demo
          image: keda-php-demo:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: keda-php-demo
  namespace: keda-demo
spec:
  selector:
    app: keda-php-demo
  ports:
    - port: 80
      targetPort: 8080